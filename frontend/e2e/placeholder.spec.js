// Placeholder E2E test — actual Playwright tests will be added in a future stage.
// This file exists so the CI E2E job has at least one test to run.
import { test, expect } from '@playwright/test';

test('placeholder — no E2E tests yet', async ({ page }) => {
  // This test is intentionally skipped until Playwright E2E tests are written.
  test.skip();
});
